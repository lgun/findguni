package com.findguni.repository;

import com.findguni.model.PlayAttempt;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlayAttemptRepository extends JpaRepository<PlayAttempt, Long> {
}
